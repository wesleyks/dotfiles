interface:
  display_name: "Create Spec"
  short_description: "Turn ideas into implementation specs"
  default_prompt: "Use $create-spec to turn this rough request into a clear implementation-ready spec, store it at docs/features/in-progress/<folder>/spec.md using a user-provided or inferred folder, and identify any missing decisions first."
